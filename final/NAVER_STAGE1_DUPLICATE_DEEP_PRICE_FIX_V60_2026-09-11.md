# 네이버 1단계 수정 V60

- 확장프로그램 버전: 2.47.0
- 그룹 이름이 달라도 옵션값 전체가 같으면 동일 그룹으로 판단해 하나만 표시합니다.
- 일반옵션과 추가옵션은 서로 섞지 않고 각각의 영역 안에서만 중복 제거합니다.
- 화면 수집이 2단 조합만 얻었더라도 내부 상품 데이터에 3단 조합이 있으면 더 깊은 조합을 우선합니다.
- `종류 → 색상 → 호수` 조합과 마지막 호수의 `+/-` 추가금액을 함께 보존합니다.
- 기존 최대 2회 확인, 전체 최대 2분 및 기존 DB 구조는 유지합니다.
